# Clerk — magic-link auth
# Get these from https://dashboard.clerk.com → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Webhook signing secret for lazy-claim user.created events
# (Clerk Dashboard → Webhooks → endpoint → "Signing Secret")
CLERK_WEBHOOK_SIGNING_SECRET=

# Neon Postgres
# Get the pooled connection string from https://console.neon.tech
DATABASE_URL=

# Platform admins (godmode across all trips). Comma-separated emails.
PLATFORM_ADMIN_EMAILS=

{
  "name": "buddycup",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "lint": "npm run lint -w web",
    "test": "npm test -w web",
    "test:watch": "npm run test:watch -w web",
    "seed:scenarios": "npm run seed:scenarios -w web"
  },
  "engines": {
    "node": "20.x"
  }
}
